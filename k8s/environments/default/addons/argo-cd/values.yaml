redis-ha:
  enabled: true

controller:
  replicas: 1

server:
  extraArgs:
    - --insecure
  httproute:
    enabled: true
    parentRefs:
      - name: shared-public-alb
        namespace: infra
        sectionName: https
    hostnames:
      - "argocd.demosfelipetrindade.lat"
